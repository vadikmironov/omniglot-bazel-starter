# Patches applied to Java bazel_deps via single_version_override in
# java_segment.MODULE.bazel. Exported so MODULE.bazel can reference them
# by label.

package(default_visibility = ["//visibility:public"])

exports_files(glob(["*.patch"]))
