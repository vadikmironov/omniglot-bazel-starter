# Patches applied to publish-related bazel_deps via single_version_override in
# publish_segment.MODULE.bazel. Exported so MODULE.bazel can reference them by
# label.

package(default_visibility = ["//visibility:public"])

exports_files(glob(["*.patch"]))
