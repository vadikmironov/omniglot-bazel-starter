# Patches applied to lint bazel_deps via single_version_override in
# MODULE.bazel. Exported so MODULE.bazel can reference them by label.

package(default_visibility = ["//visibility:public"])

exports_files(glob(["*.patch"]))
