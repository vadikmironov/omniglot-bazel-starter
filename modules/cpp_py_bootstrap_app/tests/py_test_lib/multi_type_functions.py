def add_numbers(a: int, b: int) -> int:
    return a + b


def concatenate(prefix: str, value: int, suffix: str) -> str:
    return f"{prefix}{value}{suffix}"


def is_positive(value: float) -> bool:
    return value > 0.0


def multiply(a: float, b: float) -> float:
    return a * b
